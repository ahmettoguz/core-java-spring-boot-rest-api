{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "parallel": "mocha '**/*.spec.ts' --parallel --reporter mochawesome --require mochawesome/register",
    "test": "mocha '**/*.spec.ts' --reporter mochawesome",
    "spec": "mocha './test/spec/health-check.spec.ts' --reporter mochawesome",
    "grep": "npm run test -- --grep"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/chai": "^4.3.14",
    "@types/mocha": "^10.0.6",
    "@types/node": "^20.12.2",
    "axios": "^1.6.8",
    "chai": "^5.1.0",
    "mocha": "^10.4.0",
    "mochawesome": "^7.1.3"
  }
}
