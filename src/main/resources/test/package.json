{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "parallel": "mocha '**/*.spec.js' --parallel --reporter mochawesome --require mochawesome/register",
    "test": "mocha '**/*.spec.js' --reporter mochawesome",
    "spec": "mocha './test/spec/health-check.spec.js' --reporter mochawesome",
    "grep": "npm run test -- --grep"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.8",
    "chai": "^5.1.0",
    "mocha": "^10.4.0",
    "mochawesome": "^7.1.3"
  }
}
