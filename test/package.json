{
  "name": "api-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "cd C:\\Users\\lenovo\\Desktop\\repository\\Repository_Backup\\repositoryBackup\\public\\Spring-Boot-Rest-Api\\database && docker-compose build db-initializer && docker-compose up -d db-initializer",
    "parallel": "mocha '**/*.spec.ts' --parallel --reporter mochawesome --require mochawesome/register --timeout 30000",
    "test": "cls && ts-node ./node_modules/mocha/bin/_mocha '**/*.spec.ts' './src/hook/sync.hook.ts' --reporter mochawesome --timeout 30000 && npm run clean",
    "js": "cls && mocha '**/*.spec.ts' './src/hook/sync.hook.ts' --reporter mochawesome --timeout 30000",
    "grep": "npm run test -- --grep"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/mocha": "^10.0.6",
    "@types/node": "^20.12.2",
    "axios": "^1.6.8",
    "mocha": "^10.4.0",
    "mochawesome": "^7.1.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.4"
  }
}
