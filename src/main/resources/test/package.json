{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "parallel": "mocha '**/*.spec.ts' --parallel --reporter mochawesome --require mochawesome/register --timeout 30000",
    "test": "mocha '**/*.spec.ts' './src/hook/sync.hook.ts' --reporter mochawesome --timeout 30000",
    "spec": "mocha './src/spec/health-check/health-check.spec.ts' './src/hook/sync.hook.ts' --reporter mochawesome --timeout 30000",
    "grep": "npm run test -- --grep"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/mocha": "^10.0.6",
    "@types/node": "^20.12.2",
    "axios": "^1.6.8",
    "mocha": "^10.4.0",
    "mochawesome": "^7.1.3"
  }
}
